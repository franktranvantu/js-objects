const person = {
  name: 'Frank',
  city: 'Vietnam',
  age: 29,
  isStudent: true,
  skills: ['HTML', 'CSS', 'JAVASCRIPT']
}

const message = `Hi, I'm ${person.name}. I live in ${person.city}.`;
console.log(message);